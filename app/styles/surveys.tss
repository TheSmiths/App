// header

".header": {
    height: 150,
    backgroundColor: '#01CBE1',
    top: 0
}

".header[platform=android]": {
    height: 100
}

".headerWelcomeTitle": {
    text: L('surveys.welcomeTitle'),
    top: 35,
    font: {
        fontSize: 17,
        fontFamily: 'ProximaNova-Semibold',
    },
    color: Alloy.CFG.design.header.fontColor,
    textAlign: 'center'
}

// Header - Upload button
"#uploadButtonContainer": {
    opacity: 0.3
}

"#uploadButtonContainer[platform=ios]": {
    top: 20,
    right: 0,
    width: 60,
    height: 40,
}

".uploadButton": {
    image: '/images/navigation/upload.png',
    height: 17,
    width: 17,
}

".uploadButton[platform=ios]": {
    right: 20
}

"#notificationContainer":{
    width: 17,
    height: 17,
    zIndex: 10,
    visible: false
}

"#notificationContainer[platform=ios]": {
    top: 2,
    right: 10,
}

"#notificationContainer[platform=android]": {
    visible: true,
    right: 1,
    bottom: 1
}


"#notificationCount": {
    text: "1",
    height: 17,
    width: 16,
    zIndex: 20,
    color: '#fff',
    font: {
        fontSize: 11,
        fontWeight: 'bold'
    },
    textAlign: 'center',
}
"#notificationCount[platform=ios]": {
    left: 1,
}

".notificationBackground": {
    image: '/images/notifications/notification.png',
    width: 17,
    height: 17,
} 


// Buttons

"#startSurvey": {
    top: 75,
    text: L('surveys.startSurvey'),
    textAlign: 'center',
    borderRadius: 0,
    borderWidth: 3,
    borderColor: '#fff',
    backgroundColor: "#fff",
    height: 50,
    color: "#01CBE1",
    font: {
        fontSize: 16,
        fontFamily: 'ProximaNova-Semibold',
    }
}

"#startSurvey[platform=android]": {
    top: 20
}

".content": {
    top: 150,
    backgroundColor: '#F0F0F0'
}

".content[platform=android]": {
    top: 100
}

"#emptyView": {
    height: Ti.UI.SIZE,
    top: 25,
    bottom: 25,
    layout: 'vertical'
}

".or": {
    top: 0,
    text: L('surveys.or'),
    textAlign: 'center',
    font: {
        fontSize: 12,
        fontWeight: "normal"
    },
    color: "#B9B9B9",
    left: 25,
    right: 25,
}

"#viewGuide": {
    top: 15,
    borderColor: "#B9B9B9",
    color: "#B9B9B9",
    text: L('surveys.viewGuide'),
    textAlign: 'center'
}


// Profile

"#surveyTableView": {
    top: 10,
    visible: false,
    backgroundColor: 'transparent'
}

"#surveyTableView[platform=ios]": {
    separatorStyle: Titanium.UI.iPhone.TableViewSeparatorStyle.NONE,
}



