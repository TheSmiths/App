"TableViewRow": {
    height: 60,
    backgroundColor: 'transparent',
    selectedBackgroundColor: '#fff'
}

".guideContainer": {
    top: 5,
    bottom: 0,
    left: 5,
    right: 5,
    height: 55,
    backgroundColor: '#fff'
}


"#label": {
    left: 15,
    color: Alloy.CFG.design.colors.mediumBlue,
    font: {
        fontWeight: 'bold',
        fontSize: 12
    }
}

".icon": {
    right: 15,
    width: 6,
    height: 11,
    image: '/images/buttons/buttonArrow.png'
}
