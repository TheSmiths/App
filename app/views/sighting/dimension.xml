<Alloy>
    <Window id="dimension">
        <View class="header">
            <Require src="components/backButton" id="backButton" onClick="onClickBackButton" platform="ios"/>
            <Label id="headerTitle" class="headerTitle" platform="ios"/>
            <Label id="headerSubTitle" class="headerSubTitle" />
            <Require src="components/guideButton" id="guideButton" topic="dimension" platform="ios"/>
        </View>

        <Menu platform="android">
            <Require src="components/guideButton" id="guideButton" topic="dimension" />
        </Menu>

        <ScrollView class="content">
            <View class="gridContainer">
                <Require id="grid" src="components/grid" onClick="onClickGrid" />
            </View>
        </ScrollView>
    </Window>
</Alloy>
