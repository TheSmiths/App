<Alloy>
    <Window id="material">
        <View class="header">
             <Require src="components/closeButton" onClick="closeWindow" />
            <Label id="headerTitle" class="headerTitle" />
            <Label id="headerSubTitle" class="headerSubTitle" />
            <Require src="components/guideButton" id="guideButton" topic="material" />
        </View>

        <ScrollView class="content">
            <View class="gridContainer">
                <Require id="grid" src="components/grid" onClick="onClickGrid" />
            </View>
        </ScrollView>
    </Window>
</Alloy>
